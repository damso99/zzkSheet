import { lazy, Suspense, useEffect, useLayoutEffect, useMemo, useRef, useState } from "react";
import styles from "./RaidSchedulePage.module.css";
import AuctionBidSidebar from "./components/AuctionBidSidebar.jsx";
import RaidCard from "./components/RaidCard.jsx";
import { formatDateLabel, formatLocalDateTime, getScheduleStartAt, getTodayIsoDate, isInScheduleRange } from "./utils/dateUtils.js";
import { buildFallbackRaidSchedule, buildRaidSchedule } from "./utils/raidParser.js";
import { findUpdatedRaidKeys, getRaidTrackingKey, markRaidAsSeen } from "./utils/raidUpdateTracker.js";
import { DEFAULT_SHEET_URL, DEFAULT_TARGET_GID, loadRaidSheetBundle } from "./utils/sheetApi.js";

const loadAuctionBidCalculator=()=>import("./components/AuctionBidCalculator.jsx"); const loadCharacterDetailModal=()=>import("./components/CharacterDetailModal.jsx"); const loadPersonalRaidPage=()=>import("../PersonalRaidPage/PersonalRaidPage.jsx"); const loadPersonalSchedulePage=()=>import("../PersonalSchedulePage/PersonalSchedulePage.jsx");
const AuctionBidCalculator=lazy(loadAuctionBidCalculator),CharacterDetailModal=lazy(loadCharacterDetailModal),PersonalRaidPage=lazy(loadPersonalRaidPage),PersonalSchedulePage=lazy(loadPersonalSchedulePage);
const TAB_ORDER=["today","week","auction","personalRaid","personal"]; const TAB_LABELS={today:"금일 일정",week:"주간 일정",auction:"쌀산기",personal:"개인 일정",personalRaid:"레이드 참여 현황"};
export default function RaidSchedulePage({initialTab="today"}){const[activeTab,setActiveTab]=useState(initialTab),[weeklyViewMode,setWeeklyViewMode]=useState("list"),[raids,setRaids]=useState([]),[isLoading,setIsLoading]=useState(true),[errorMessage,setErrorMessage]=useState(""),[selectedCharacterName,setSelectedCharacterName]=useState(""),[selectedOwnerName,setSelectedOwnerName]=useState(""),[selectedWeeklyParticipant,setSelectedWeeklyParticipant]=useState(""),[selectedCalendarRaid,setSelectedCalendarRaid]=useState(null),[updatedRaidKeys,setUpdatedRaidKeys]=useState(()=>new Set());const raidSignatureRef=useRef("");const[sourceMeta,setSourceMeta]=useState({isFallback:false,fetchedAt:"",sourceUrl:DEFAULT_SHEET_URL});const todayIsoDate=useMemo(()=>getTodayIsoDate(),[]),showOverviewStats=activeTab==="today"||activeTab==="week";
useEffect(()=>{setActiveTab(initialTab)},[initialTab]);useEffect(()=>{const p=()=>{loadAuctionBidCalculator();loadCharacterDetailModal();loadPersonalRaidPage();loadPersonalSchedulePage()};const i=window.requestIdleCallback?.(p,{timeout:3000}),t=i==null?window.setTimeout(p,1500):null;return()=>{if(i!=null)window.cancelIdleCallback?.(i);if(t!=null)window.clearTimeout(t)}},[]);useEffect(()=>{let ignore=false,flight=false;const controller=new AbortController();async function loadSchedule({background=false}={}){if(flight)return;flight=true;if(!background){setIsLoading(true);setErrorMessage("")}try{const bundle=await loadRaidSheetBundle({forceRefresh:background,signal:controller.signal,sheetUrl:DEFAULT_SHEET_URL,targetGid:DEFAULT_TARGET_GID}),normalized=buildRaidSchedule(bundle),sig=JSON.stringify(normalized);if(!ignore){if(raidSignatureRef.current!==sig){raidSignatureRef.current=sig;setUpdatedRaidKeys(findUpdatedRaidKeys(normalized));setRaids(normalized)}setSourceMeta({fetchedAt:bundle.fetchedAt,isFallback:false,sourceUrl:bundle.sourceUrl});setErrorMessage("")}}catch(error){if(ignore||background)return;setRaids(buildFallbackRaidSchedule(todayIsoDate));setSourceMeta({fetchedAt:formatLocalDateTime(new Date()),isFallback:true,sourceUrl:DEFAULT_SHEET_URL});setErrorMessage(error instanceof Error?`시트 로딩에 실패했습니다. 잠시 후 다시 시도해주세요. ${error.message}`:"시트 로딩에 실패했습니다. 잠시 후 다시 시도해주세요.")}finally{flight=false;if(!ignore&&!background)setIsLoading(false)}}loadSchedule();const refresh=()=>{if(document.visibilityState==="visible")loadSchedule({background:true})},timer=window.setInterval(refresh,30000);document.addEventListener("visibilitychange",refresh);return()=>{window.clearInterval(timer);document.removeEventListener("visibilitychange",refresh);controller.abort();ignore=true}},[todayIsoDate]);
function handleRaidOpen(raid){markRaidAsSeen(raid);setUpdatedRaidKeys(c=>{const n=new Set(c);n.delete(getRaidTrackingKey(raid));return n})}const todayRaids=useMemo(()=>raids.filter(r=>isInScheduleRange(r,todayIsoDate)),[raids,todayIsoDate]),todayOwnerNames=useMemo(()=>getOwnerNames(todayRaids),[todayRaids]),weeklyParticipantNames=useMemo(()=>getOwnerNames(raids),[raids]),groupedRaids=useMemo(()=>groupRaidsByDate(raids),[raids]),todayStartTime=useMemo(()=>getEarliestRaidTime(todayRaids),[todayRaids]),q=selectedWeeklyParticipant.trim();const weeklySelectedResults=useMemo(()=>{if(!q)return[];const m=new Map();raids.forEach(r=>r.participants.filter(p=>p.ownerName===q).forEach(p=>{const k=`${r.date}-${p.ownerName}-${p.characterName}`,e=m.get(k);if(e){if(!e.raids.includes(r.raidName))e.raids.push(r.raidName);return}m.set(k,{date:r.date,id:k,ownerName:p.ownerName,characterName:p.characterName,raids:[r.raidName],time:r.time})}));return[...m.values()]},[raids,q]);const searchGroups=useMemo(()=>groupItemsByDate(weeklySelectedResults),[weeklySelectedResults]),weeklyCalendarDays=useMemo(()=>buildWeeklyCalendarDays(raids),[raids]),sortedTodayRaids=useMemo(()=>[...todayRaids].sort(compareRaidOrder),[todayRaids]);
return <div className={styles.page}><div className={styles.backdrop}/><div className={styles.content}><header className={styles.hero}><div><p className={styles.eyebrow}>LostArk Weekly Planner</p><h1>Stick Over Flow</h1><div className={styles.metaLine}><span>갱신 {formatFetchedAt(sourceMeta.fetchedAt)}</span><span className={`${styles.connectionStatus} ${sourceMeta.isFallback?styles.connectionOffline:styles.connectionOnline}`}><span className={styles.connectionStatusDot}/>{sourceMeta.isFallback?"Disconnected":"Connected"}</span></div></div></header>{showOverviewStats?<section className={styles.toolbar}><div className={styles.tabs} role="tablist" aria-label="일정 보기 선택">{TAB_ORDER.map(k=><button key={k} type="button" role="tab" aria-selected={activeTab===k} className={activeTab===k?styles.activeTab:styles.tab} onClick={()=>setActiveTab(k)}>{TAB_LABELS[k]}</button>)}</div><div className={styles.summaryChips}><div className={styles.chip}><span>금일 일정</span><strong>{todayRaids.length}개</strong></div><div className={styles.chip}><span>전체 일정</span><strong>{raids.length}개</strong></div><div className={styles.chip}><span>전체 캐릭</span><strong>{countUniqueCharacters(raids)}명</strong></div></div></section>:null}<main>{!isLoading&&activeTab==="today"?<section className={`${styles.section} ${styles.pageSection} ${styles.todaySchedule}`}><SectionHeading styles={styles} title={TAB_LABELS.today} subtitle={`${formatDateLabel(todayIsoDate)} 기준 일정`} meta={<TimeMetaBadge styles={styles} value={todayStartTime}/>}/><TodayParticipantList ownerNames={todayOwnerNames} selectedOwnerName={selectedOwnerName} onSelectOwnerName={setSelectedOwnerName} styles={styles}/>{sortedTodayRaids.length===0?<StatePanel styles={styles} message="금일 일정이 없습니다."/>:<div className={styles.cardGrid}>{sortedTodayRaids.map(r=><RaidCard key={r.id} raid={r} styles={styles} onCharacterClick={setSelectedCharacterName} collapsible isHighlighted={Boolean(selectedOwnerName)&&r.participants.some(p=>p.ownerName===selectedOwnerName)} selectedOwnerName={selectedOwnerName} isUpdated={updatedRaidKeys.has(getRaidTrackingKey(r))} onOpen={handleRaidOpen}/>)}</div>}</section>:null}{!isLoading&&activeTab==="week"?<section className={`${styles.section} ${styles.pageSection}`}><SectionHeading styles={styles} title={TAB_LABELS.week} subtitle="요일별 레이드 일정" meta={<WeekViewToggle styles={styles} value={weeklyViewMode} onChange={setWeeklyViewMode}/>}/><WeeklyParticipantList ownerNames={weeklyParticipantNames} selectedOwnerName={selectedWeeklyParticipant} onSelectOwnerName={setSelectedWeeklyParticipant} styles={styles}/>{weeklyViewMode==="calendar"?(weeklyCalendarDays.some(d=>d.items.length)?<div className={styles.weekCalendarGrid}>{weeklyCalendarDays.map(d=><section key={d.dateKey} className={styles.weekCalendarDay}><header className={styles.weekCalendarDayHeader}><div className={styles.weekCalendarDayHeaderMain}><span className={styles.weekCalendarDayLabel}>{d.label}</span><strong className={styles.weekCalendarDayDate}>{d.dateLabel}</strong></div><span className={styles.weekCalendarDayStartTime}>{d.startTime||"시간 미정"}</span></header>{d.items.length?<div className={styles.weekCalendarDayList}>{d.items.map(r=><article key={r.id} className={`${styles.weekCalendarDayCard} ${Boolean(selectedWeeklyParticipant)&&r.participants.some(p=>p.ownerName===selectedWeeklyParticipant)?styles.weekCalendarDayCardHighlighted:""}`} onClick={()=>setSelectedCalendarRaid(r)} role="button" tabIndex={0}><div className={styles.weekCalendarDayCardTop}><strong className={styles.weekCalendarDayCardTitle}>{r.raidName}</strong></div></article>)}</div>:<p className={styles.weekCalendarDayEmpty}>일정 없음</p>}</section>)}</div>:<StatePanel styles={styles} message="이번 주 일정이 없습니다."/>):q?(searchGroups.length===0?<StatePanel styles={styles} message="검색 결과가 없습니다."/>:<div className={styles.weekStack}>{searchGroups.map(g=><details key={g.id} className={styles.dayGroup}><summary className={styles.dayHeader}><span className={styles.dayTitle}>{g.label}</span><span className={styles.dayHeaderMeta}><TimeMetaBadge styles={styles} value={formatGroupTime(g)} className={styles.dayTimeBadge}/><span>{g.items.length}개 결과</span></span></summary><div className={`${styles.searchResults} ${styles.weeklySearchResult}`}>{g.items.map(i=><article key={i.id} className={styles.searchResultCard}><div className={styles.searchInlineMeta}><div className={`${styles.searchInlineField} ${styles.searchCharacterField}`}><span>참여 캐릭터</span><button type="button" className={styles.searchCharacterButton} onClick={()=>setSelectedCharacterName(i.characterName)}>{i.characterName}</button></div><div className={`${styles.searchInlineField} ${styles.searchOwnerField}`}><span>이름</span><strong>{i.ownerName}</strong></div></div><div className={styles.searchRaidList}>{i.raids?.map(r=><span key={`${i.id}-${r}`} className={styles.searchRaidPill}>{r}</span>)}</div></article>)}</div></details>)}</div>):groupedRaids.length===0?<StatePanel styles={styles} message="일정이 없습니다."/>:<div className={styles.weekStack}>{groupedRaids.map(g=><details key={g.id} className={styles.dayGroup}><summary className={styles.dayHeader}><span className={styles.dayTitle}>{g.label}</span><span className={styles.dayHeaderMeta}><TimeMetaBadge styles={styles} value={formatGroupTime(g)} className={styles.dayTimeBadge}/><span>{g.items.length}개 일정</span></span></summary><div className={styles.cardGrid}>{g.items.map(r=><RaidCard key={r.id} raid={r} styles={styles} onCharacterClick={setSelectedCharacterName} isHighlighted={Boolean(selectedWeeklyParticipant)&&r.participants.some(p=>p.ownerName===selectedWeeklyParticipant)} selectedOwnerName={selectedWeeklyParticipant}/>)}</div></details>)}</div>}</section>:null}{activeTab==="auction"?<section className={`${styles.section} ${styles.pageSection}`}><Suspense fallback={<StatePanel styles={styles} message="쌀산기를 불러오는 중입니다."/>}><AuctionBidCalculator/></Suspense></section>:null}{activeTab==="personal"?<section className={`${styles.section} ${styles.pageSection}`}><Suspense fallback={<StatePanel styles={styles} message="개인 일정을 불러오는 중입니다."/>}><PersonalSchedulePage embedded/></Suspense></section>:null}{activeTab==="personalRaid"?<section className={`${styles.section} ${styles.pageSection}`}><Suspense fallback={<StatePanel styles={styles} message="레이드 참여 현황을 불러오는 중입니다."/>}><PersonalRaidPage embedded/></Suspense></section>:null}</main>{(activeTab==="today"||activeTab==="week")?<aside className={styles.auctionFloatingSidebar}><AuctionBidSidebar/></aside>:null}</div>{selectedCharacterName?<Suspense fallback={null}><CharacterDetailModal characterName={selectedCharacterName} onClose={()=>setSelectedCharacterName("")} styles={styles}/></Suspense>:null}{selectedCalendarRaid?<CalendarRaidModal raid={selectedCalendarRaid} onCharacterClick={setSelectedCharacterName} onClose={()=>setSelectedCalendarRaid(null)} styles={styles}/>:null}</div>}
function CalendarRaidModal({raid,onCharacterClick,onClose,styles}){const ref=useRef(onClose);ref.current=onClose;useLayoutEffect(()=>{const y=window.scrollY,prev={overflow:document.body.style.overflow,position:document.body.style.position,top:document.body.style.top,width:document.body.style.width};document.body.style.overflow="hidden";document.body.style.position="fixed";document.body.style.top=`-${y}px`;document.body.style.width="100%";const key=e=>{if(e.key==="Escape")ref.current()};window.addEventListener("keydown",key);return()=>{window.removeEventListener("keydown",key);Object.assign(document.body.style,prev);window.scrollTo(0,y)}},[]);return <div className={styles.modalOverlay} onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div className={styles.modalShell} role="dialog" aria-modal="true"><button type="button" className={styles.modalCloseButton} onClick={onClose}>×</button><RaidCard raid={raid} styles={styles} onCharacterClick={onCharacterClick}/></div></div>}
function StatePanel({styles,message}){return <div className={styles.statePanel}>{message}</div>}function TimeMetaBadge({styles,value,className=""}){return <span className={`${styles.timeMetaBadge} ${className}`}>{value||"시간 미정"}</span>}function WeekViewToggle({styles,value,onChange}){return <div className={styles.weekViewToggle}><button type="button" className={`${styles.weekViewToggleButton} ${value==="list"?styles.weekViewToggleButtonActive:""}`} onClick={()=>onChange("list")}>목록</button><button type="button" className={`${styles.weekViewToggleButton} ${value==="calendar"?styles.weekViewToggleButtonActive:""}`} onClick={()=>onChange("calendar")}>캘린더</button></div>}function SectionHeading({styles,title,subtitle,meta}){return <div className={styles.sectionHeading}><div className={styles.sectionHeadingTopRow}><div><h2>{title}</h2><p>{subtitle}</p></div>{meta}</div></div>}function TodayParticipantList({ownerNames,selectedOwnerName,onSelectOwnerName,styles}){return <div className={styles.todayParticipantPanel}><div className={styles.todayParticipantHeader}><span>이름 선택</span></div><div className={styles.todayParticipantBadges}>{ownerNames.map(n=><button key={n} type="button" className={selectedOwnerName===n?styles.activeTodayParticipantBadge:styles.todayParticipantBadge} onClick={()=>onSelectOwnerName(selectedOwnerName===n?"":n)}>{n}</button>)}</div></div>}function WeeklyParticipantList(props){return <TodayParticipantList {...props}/>}function getOwnerNames(items){return[...new Set(items.flatMap(r=>r.participants.map(p=>p.ownerName)).filter(Boolean))].sort((a,b)=>a.localeCompare(b,"ko"))}function countUniqueCharacters(items){return new Set(items.flatMap(r=>r.participants.map(p=>p.characterName)).filter(Boolean)).size}function getEarliestRaidTime(items){return items.map(r=>r.time).filter(Boolean).sort()[0]||""}function compareRaidOrder(a,b){return(getScheduleStartAt(a)?.getTime()||0)-(getScheduleStartAt(b)?.getTime()||0)}function groupRaidsByDate(items){const m=new Map();items.forEach(r=>{if(!m.has(r.date))m.set(r.date,{id:r.date,label:formatDateLabel(r.date),items:[]});m.get(r.date).items.push(r)});return[...m.values()].sort((a,b)=>a.id.localeCompare(b.id))}function groupItemsByDate(items){const m=new Map();items.forEach(i=>{if(!m.has(i.date))m.set(i.date,{id:i.date,label:formatDateLabel(i.date),items:[]});m.get(i.date).items.push(i)});return[...m.values()]}function formatGroupTime(g){return g.items?.[0]?.time||""}function buildWeeklyCalendarDays(items){return groupRaidsByDate(items).map(g=>({dateKey:g.id,label:new Intl.DateTimeFormat("ko-KR",{weekday:"short"}).format(new Date(`${g.id}T12:00:00`)),dateLabel:formatDateLabel(g.id),startTime:getEarliestRaidTime(g.items),items:g.items}))}function formatFetchedAt(v){return v||"-"}
